# findspace_leadmanager
create a virtual env <br />
cd to the correct directory <br />
run pip install -r requirement.txt <br />
run npm install <br />
run python manage.py makemigrations <br />
run python manage.py migrate <br />
run npm dev (go into the package.json file and remove --watch from dev script to stop it from watching) <br />
run python manage.py runserver <br />
